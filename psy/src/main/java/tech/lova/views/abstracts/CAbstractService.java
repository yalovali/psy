package tech.lova.views.abstracts;

public class CAbstractService<CEntity> {
	/*
	 * protected final CAbstractRepository<CEntity> repository;
	 * 
	 * public CAbstractService(final CAbstractRepository<CEntity> repository) {
	 * this.repository = repository; // Initialize the repository for data access }
	 * 
	 * public int count() { return (int) repository.count(); }
	 * 
	 * public void delete(final Long id) { repository.deleteById(id); }
	 * 
	 * public Optional<CEntity> get(final Long id) { return repository.findById(id);
	 * }
	 * 
	 * public Page<CEntity> list(final Pageable pageable) { return
	 * repository.findAll(pageable); }
	 * 
	 * public Page<CEntity> list(final Pageable pageable, final
	 * Specification<CEntity> filter) { return repository.findAll(filter, pageable);
	 * }
	 * 
	 * public CEntity save(final CEntity entity) { return repository.save(entity); }
	 */
}
